-- Initial migration. Replace the placeholder below with your first schema, or
-- add new files (0002_*.sql, 0003_*.sql, ...) — goose applies them in order.
-- +goose Up
-- +goose StatementBegin
SELECT 1;
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
SELECT 1;
-- +goose StatementEnd
